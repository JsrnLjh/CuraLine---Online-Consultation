# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Configuration
# For local development: mongodb://localhost:27017/curaline
# For production (MongoDB Atlas): mongodb+srv://username:password@cluster.mongodb.net/curaline
MONGODB_URI=mongodb://localhost:27017/curaline

# Client URL (Frontend URL)
# For local: http://localhost:3000
# For production: https://your-app-name.onrender.com
CLIENT_URL=http://localhost:3000

# Email Configuration (Optional - works without this in simulation mode)
EMAIL_PROVIDER=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM_NAME=CuraLine Health

# Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to Git (it's in .gitignore)
# 3. For Render.com, add these as Environment Variables in dashboard
# 4. Email is optional - system works in simulation mode without it
